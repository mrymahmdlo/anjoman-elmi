export const Activity = (item) => {
    return(
        <>
            <p> شماره تماس مشتری : {item["شماره تماس مشتری"]}</p>
            <p> شماره تماس پشتیبان :{item["شماره تماس پشتیبان"]}</p>
        </>
    );
}